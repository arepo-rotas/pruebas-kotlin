<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pacdesivanvishneuski">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PacDesIvanVishneuski">
        <activity android:name=".control"></activity>

        <service
            android:name=".Service"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".Entity"
            android:label="@string/title_activity_entidad"
            android:theme="@style/Theme.PacDesIvanVishneuski.NoActionBar" />
        <activity android:name=".Consultar" />
        <activity android:name=".InsertarDatos" />
        <activity android:name=".CrearTabla" />
        <activity android:name=".MainActivity3" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>